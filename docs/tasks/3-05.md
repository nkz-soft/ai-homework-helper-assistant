**Implement `normalize` node (evidence model)**
  - File: `packages/orchestrator/src/graph/nodes/normalize.py`
  - **Cursor prompt:**  
    - Transform retrieved items into a unified `evidence[]`: claim, short excerpt, source, url/id, license_note, confidence. Ensure excerpts are short to avoid copying.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` after test is complete.
  - **Definition of Done:** 
    - synthesis step can rely only on `evidence`.
    - all unit tests and linters (ruff, mypy) must pass successfully.
