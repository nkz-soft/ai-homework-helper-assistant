**Add tool-call interceptors: budget + throttling**
  - File: `packages/mcp_clients/src/interceptors/budget.py`
  - **Cursor prompt:**  
    - Implement a budget manager limiting: total tool calls per request, per-server call caps (esp. StackOverflow), max payload bytes. Raise a `ToolBudgetExceeded` exception. Use all cursor rules from `.cursor/rules`.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools (80-code-style-python.mdc) after test is complete.”
  - **Definition of Done:** 
    - retrieval step respects budgets deterministically.
    - all unit tests must pass successfully.