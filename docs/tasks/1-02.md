**Implement MCP config loader + client factory**
  - Package: `packages/mcp_clients`
  - File: `packages/mcp_clients/src/client_factory.py`
  - **Cursor prompt:**  
    “Implement a `McpClientFactory` that loads `config/mcp_servers.*.json`, instantiates a Multi-Server MCP client (LangChain adapter), and exposes `get_tools()` / `get_resources()` / `get_prompts()` methods. Add basic timeout + retry settings and structured logging.”
  - **Definition of Done:** calling code can obtain tool handles for each MCP server.
  