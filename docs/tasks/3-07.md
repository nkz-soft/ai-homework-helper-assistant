**Implement `synthesize` node**
  - File: `packages/orchestrator/src/graph/nodes/synthesize.py`
  - **Cursor prompt:**  
    - Generate a structured answer: explanation → steps → worked example → self-check questions → sources. Use only evidence-backed claims; include citations metadata.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` after test is complete.
  - **Definition of Done:** 
    - output includes both `final_answer` and `citations`. 
    - all unit tests and linters (ruff, mypy) must pass successfully.    