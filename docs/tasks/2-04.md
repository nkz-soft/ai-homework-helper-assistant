**Add tool-call interceptors: caching**
  - File: `packages/mcp_clients/src/interceptors/caching.py`
  - **Cursor prompt:**  
    “Implement a budget manager limiting: total tool calls per request, per-server call caps (esp. StackOverflow), max payload bytes. Raise a `ToolBudgetExceeded` exception. Always write unit tests for the functionality being developed. Use all cursor rules from `.cursor/rules`.
    Automatically run unit tests after task is complete.
    Automatically run format and style tools (80-code-style-python.mdc) after test is complete.”
  - **Definition of Done:** 
    - repeated calls with same key do not hit MCP server.
    - all unit tests must pass successfully.
 