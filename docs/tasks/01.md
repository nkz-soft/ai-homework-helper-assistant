- **Initialize monorepo folder structure**
  - Create: `apps/`, `packages/`, `config/`, `docs/`, `tests/`, `infra/`, `.github/workflows/`, `scripts/`
  - **Cursor prompt:**  
    “Create the monorepo directory structure for `homework-helper-mcp` exactly as described in `docs/architecture.md` (apps/packages/config/docs/tests/infra/scripts/.github). Add placeholder `__init__.py` where needed. Don’t implement logic yet—only scaffolding.”
  - **Definition of Done:** tree exists; imports won’t fail due to missing packages
