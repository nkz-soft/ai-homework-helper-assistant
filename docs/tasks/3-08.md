**Implement `self_check` node**
  - File: `packages/orchestrator/src/graph/nodes/self_check.py`
  - **Cursor prompt:**  
    - Implement a verifier that checks: each key claim maps to evidence. If not, downgrade claim or remove it. Produce diagnostics for missing support.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` after test is complete.
  - **Definition of Done:** 
    - unsupported claims are not present in the final answer.
    - all unit tests and linters (ruff, mypy) must pass successfully.