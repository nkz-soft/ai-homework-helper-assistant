**Implement `safety` node (prompt injection + academic integrity)**
  - File: `packages/orchestrator/src/graph/nodes/safety.py`
  - **Cursor prompt:**  
    - Implement safety checks: detect prompt injection in retrieved text; detect ‘do my homework for me’ requests; apply `coach` policy by default. Produce `safety_flags` and adjust response mode.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` after test is complete.
  - **Definition of Done:** 
    - unsafe instructions from sources are ignored; user gets guided help.
    - all unit tests and linters (ruff, mypy) must pass successfully.