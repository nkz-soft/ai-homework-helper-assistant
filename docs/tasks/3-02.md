**Implement `classify` node**
  - File: `packages/orchestrator/src/graph/nodes/classify.py`
  - **Cursor prompt:**
    - Implement a classifier node that labels subject (programming/math/etc.), intent (explain/debug/solve), and whether clarification is needed. Output must be minimal and deterministic.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` 
  - **Definition of Done:** 
    - state has `subject`, `intent`, `needs_clarification` fields set.
    - all unit tests and linters must pass successfully.