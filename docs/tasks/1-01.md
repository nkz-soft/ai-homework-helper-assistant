**Add MCP server config files**
  - Create: `config/mcp_servers.dev.json`, `config/mcp_servers.prod.json`
  - **Cursor prompt:**  
    “Create MCP server config JSONs: `wikipedia` (stdio), `stackoverflow` via `npx mcp-remote https://github.com/NoTalkTech/stackoverflow-mcp`, and optional `textbooks` server. Provide dev defaults; keep prod as template without secrets.”
  - **Definition of Done:** JSON schema is valid; names match intended usage.
