**Add tool-call interceptors: audit**
  - File: `packages/mcp_clients/src/interceptors/audit.py`
  - **Cursor prompt:**  
    “Implement an interceptor that logs every tool call (server, tool name, args hash, latency, response size). Make logs structured JSON and redact secrets.”
  - **Definition of Done:** each tool call emits a single audit event.
  - Use all cursor rules from `.cursor/rules`
  - Always write unit tests for the functionality being developed.
  - Always run tests after task is complete using pytest --cov=src.