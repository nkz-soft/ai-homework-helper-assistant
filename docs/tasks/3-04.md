**Implement `retrieve` node (parallel tool calls)**
  - File: `packages/orchestrator/src/graph/nodes/retrieve.py`
  - **Cursor prompt:**  
    - Implement retrieval that executes planned MCP tool calls in parallel (async). Enforce tool budget and timeouts. Store raw tool outputs in `retrieved_items`.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` after test is complete.
  - **Definition of Done:** 
    - retrieval succeeds even if some tools fail (partial results). 
    - all unit tests and linters (ruff, mypy) must pass successfully.