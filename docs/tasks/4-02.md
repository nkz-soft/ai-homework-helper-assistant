**Implement subject strategy config**
  - File: `config/subjects.yaml`
  - **Cursor prompt:**  
    - Create `subjects.yaml` mapping subjects/intents to preferred sources and query heuristics (e.g., programming -> SO first; definitions -> Wikipedia).
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` 
  - **Definition of Done:** 
    - `plan` node uses these strategies.
    - all unit tests and linters (ruff, mypy) must pass successfully.