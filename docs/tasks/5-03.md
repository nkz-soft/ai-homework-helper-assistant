**Add settings, logging, caching, rate limiting**
  - Files: `apps/assistant_api/src/core/settings.py`, `logging.py`, `cache.py`, `rate_limit.py`
  - **Cursor prompt:**  
    - Add pydantic-settings config, structured logging, basic cache adapter, and per-IP rate limiting middleware.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` 
  - **Definition of Done:** 
    - configurable via env vars; logs include request id.
    - all unit tests and linters (ruff, mypy) must pass successfully.