**Bootstrap FastAPI application**
  - File: `apps/assistant_api/src/main.py`
  - **Cursor prompt:**  
    - Create a FastAPI app with `/health` and `/api/v1/chat`. Add settings loader, logging, and dependency injection for orchestrator.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` 
  - **Definition of Done:** 
    - `uvicorn` starts and health returns OK.
    - all unit tests and linters (ruff, mypy) must pass successfully.