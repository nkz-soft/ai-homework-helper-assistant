**Assemble the LangGraph workflow**
  - File: `packages/orchestrator/src/graph/build_graph.py`
  - **Cursor prompt:**  
     - Wire nodes into a LangGraph: classify → plan → retrieve → normalize → safety → synthesize → self_check → finalize. Ensure state transitions are explicit.
    - Always write unit tests for the code in a task.
    - Automatically run unit tests after task is complete.
    - Automatically run format and style tools by using `.cursor/rules/80-code-style-python.mdc` after test is complete.
  - **Definition of Done:** 
    - runnable graph with a single function `run(question, context)`.
    - all unit tests and linters (ruff, mypy) must pass successfully.