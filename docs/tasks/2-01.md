**Create ergonomic wrappers for StackOverflow tools**
  - File: `packages/mcp_clients/src/tool_wrappers.py`
  - **Cursor prompt:**  
    “Add typed wrapper functions around StackOverflow MCP tools (e.g., `so_search`, `get_content`). Normalize outputs into a common dict format and raise domain exceptions on tool errors/timeouts.”
  - **Definition of Done:** wrappers exist and are used by orchestrator retrieval node.
